# 🎯 สรุปขั้นตอนการปรับแต่งธีม MDX

การปรับแต่งการแสดงผลในเอกสาร MDX ของคุณทำได้โดยการควบคุม **3 ไฟล์หลัก** ตามฟังก์ชันที่แตกต่างกัน :

| ฟังก์ชันที่ต้องการปรับ | ไฟล์ที่ใช้ควบคุม | วิธีการแก้ไข / ตำแหน่งที่ปรับ | เหตุผล |
| :--- | :--- | :--- | :--- |
| **1. ขนาด Font Heading** (h1, h2, h3) | **`app/docs/[slug]/MdxRenderer.tsx`** | **Component Swapping:** ใช้ `h1: H1`, `h2: H2`, `h3: H3`  \nกำหนดด้วย **Tailwind Utility Classes ที่มี `!`**  \nเช่น: `className="text-3xl! font-extrabold"` | **บังคับ** Override สไตล์ของ `prose` โดยตรงที่ระดับ Component |
| **2. Code Block Padding, Font, BG Color** (`pre`) | **`app/docs/[slug]/MdxRenderer.tsx`** | **Component Swapping:** ใช้ `pre: CodeBlockWrapper`  \nกำหนดด้วย **Tailwind Utility Classes ที่มี `!`**  \nเช่น: `className="p-4 rounded-lg bg-[var(--shiki-color-background)]!"` | **บังคับ** Override สไตล์ของ `prose` สำหรับ Code Block โดยตรง |
| **3. Code Block Syntax Colors** (สีของตัวแปร, ฟังก์ชัน) | **`globals.css`** | กำหนดค่าใน **CSS Variables**  \nเช่น: `--shiki-color-background`, `--shiki-token-string`  \n(ตัวอย่างอยู่ในไฟล์ที่คุณอัปโหลด) | `rehype-pretty-code` และ Shiki ใช้ CSS Variables เหล่านี้ในการกำหนดสี Syntax |
| **4. กำหนด Base Font, BG Color ของเว็บไซต์** | **`globals.css`** | กำหนดค่าใน **Base CSS Variables**  \nเช่น: `--background`, `--foreground` และสไตล์ของ `body` | กำหนดสีพื้นฐานและ Font ทั่วไปของทั้งเว็บไซต์ |
| **5. กำหนด Theme สีรวม** (Light/Dark Mode) | **`globals.css`** | ใช้ **`@media (prefers-color-scheme: dark)`** เพื่อเปลี่ยนค่า CSS Variables ทั้งหมด (เช่น `--shiki-color-background`) | ใช้เพื่อสลับสีพื้นหลังและสี Syntax ของ Code Block ตามธีมมืด/สว่าง |

-----

## 📝 ข้อสรุปการ "ทำความสะอาด" ไฟล์ Config

เพื่อให้วิธีการใน `MdxRenderer.tsx` ทำงานได้โดยไม่มีความขัดแย้ง คุณต้องมั่นใจว่าสองไฟล์นี้ **ไม่มีโค้ด Override ที่ล้มเหลวเหลืออยู่**

### 1\. 🗑️ `tailwind.config.ts`

ปลั๊กอิน `typography` ต้องอยู่ในส่วน `plugins: [typography]` เพื่อให้ `class="prose"` ทำงานได้

### 2\. 🗑️ `globals.css`

ต้องมีเฉพาะ **Base CSS Variables** และ **Shiki CSS Variables** เท่านั้น

การทำความเข้าใจว่าต้องแก้ไข **Template Component (`MdxRenderer.tsx`)** แทนที่จะแก้ไข **Config (`tailwind.config.ts`)** คือหัวใจสำคัญในการเอาชนะความเข้มงวดของ Tailwind Typography ครับ\!
