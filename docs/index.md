# หน้าหลัก (ภาพรวมโปรเจกต์)

## 🚀 Next.js + TypeScript + Tailwind CSS Project Overview

โปรเจกต์นี้ถูกสร้างขึ้นด้วย **Next.js 16.0.3 (App Router)** โดยใช้ **TypeScript** เพื่อเพิ่มความปลอดภัยของ Type และใช้ **Tailwind CSS v4** สำหรับการจัดสไตล์ที่รวดเร็วและมีประสิทธิภาพ การ Deployment ทำบน **Vercel** ด้วยการตั้งค่าแบบ Zero-Configuration

## ✨ คุณสมบัติหลัก

* **Next.js App Router (v16.0.3)**: ใช้ระบบ Routing และ Rendering ล่าสุดของ Next.js
* **TypeScript (^5)**: การพัฒนาระบบที่มีความมั่นคงและบำรุงรักษาง่าย
* [cite_start]**Tailwind CSS v4 (^4) & PostCSS**: ใช้ `@tailwindcss/postcss` เพื่อการติดตั้งที่เรียบง่ายและประสิทธิภาพสูง [cite: 5]
* [cite_start]**ESLint (^9)**: การใช้ Flat Config (eslint.config.mts) เพื่อควบคุมคุณภาพของโค้ด [cite: 2]
* **Next.js Route Handler**: มีตัวอย่าง API Endpoint สำหรับดึงข้อมูล Contents ( `/api` ) ที่ใช้ `NextResponse.json` และการตั้งค่า Cache-Control
* [cite_start]**New JSX Transform**: ปิดการใช้งาน Rule `react/react-in-jsx-scope` เนื่องจาก Next.js 16/React 19 ใช้ New JSX Transform [cite: 4]

---

## 🛠️ เทคโนโลยีและเวอร์ชันที่ใช้

| เทคโนโลยี | เวอร์ชันที่ใช้ | รายละเอียด |
| :--- | :--- | :--- |
| **Next.js** | `16.0.3` | App Router, Server Components |
| **React/ReactDOM** | `19.2.0` | เวอร์ชันล่าสุด |
| **TypeScript** | `^5` | การตั้งค่า `moduleResolution: "bundler"` |
| **Tailwind CSS** | `^4` | ใช้ `@tailwindcss/postcss` |
| **Linter** | `ESLint ^9` | Flat Config |

---

## 🚀 การติดตั้งและรัน (Development)

ดูขั้นตอนโดยละเอียดได้ที่ [การติดตั้งและรันโปรเจกต์](setup/index.md)

1. **Clone Project**
2. **ติดตั้ง Dependencies**

    ```bash
    npm install
    ```

3. **รัน Development Server** (พร้อม **Turbopack** ที่แนะนำ)

    ```bash
    npm run dev
    # หรือใช้ Turbopack: npx next dev --turbo
    ```

</br>
