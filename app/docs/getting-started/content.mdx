// app/docs/getting-started/content.mdx

// **ไม่ต้องมี 'use client' หรือ 'export default'**
import { Alert } from '@/components/Alert'; 

// --- เนื้อหา MDX เริ่มต้นที่นี่ ---
# 💡 คู่มือเริ่มต้นใช้งาน Next.js Document Site

<p className="lead">
นี่คือเอกสารที่ถูกเรนเดอร์ด้วย **MDX** ซึ่งตอนนี้ถูกห่อหุ้มด้วย Client Component
</p>

## สถาปัตยกรรมหลัก

โปรเจกต์นี้ใช้ **Next.js 15.5.0** บน **TypeScript** และจัดสไตล์ด้วย **Tailwind CSS v4**

{/* 👈 ใช้ Alert Component ในรูปแบบ JSX Tag */}
<Alert type="info" title="Final Fix Applied">
ตอนนี้ระบบ MDX Content ของเราทำงานร่วมกับ Next.js App Router ได้อย่างถูกต้องแล้ว!
</Alert>

### คุณสมบัติของ Markdown

คุณสามารถใช้คุณสมบัติ Markdown มาตรฐานได้:

1.  รายการลำดับ
2.  รายการปกติ
3.  **ตัวหนา** และ *ตัวเอียง*

### การแสดงโค้ด

```typescript
// ตัวอย่างโค้ด TypeScript
import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  pageExtensions: ['ts', 'tsx', 'md', 'mdx'],
};