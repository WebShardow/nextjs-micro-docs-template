---
title: การเริ่มต้นใช้งาน (Getting Started)
description: คู่มือเริ่มต้นสำหรับ Next.js, Tailwind CSS และ MDX
author: DevG
date: 2025-11-24
order: 1
category: เริ่มต้นใช้งาน
---

{/* The `Alert` component is provided via the `components` mapping in `MdxRenderer` */}

# การเริ่มต้นใช้งาน (Getting Started)

นี่คือเนื้อหาเอกสารแรกของคุณที่ถูกจัดการด้วยระบบ **Dynamic MDX**
ที่รันบน **Next.js App Router** และใช้ `mdx-bundler` สำหรับการประมวลผล.

## 🎯 คุณสมบัติที่สำคัญ

* **Frontmatter:** ข้อมูล Metadata ด้านบนถูกอ่านและนำมาใช้ได้ (เช่น Title, Author) ในการทำ SEO (generateMetadata).
* **Dynamic Routing:** สามารถสร้างเอกสารเพิ่มโดยไม่ต้องแก้ไข Code.
* **Component Usage:** รองรับการใช้ React Components ภายใน MDX.

<Alert title="สำเร็จแล้ว!" type="success">
    ระบบ Dynamic MDX Content API ทำงานได้อย่างสมบูรณ์!
</Alert>

## 🚀 สรุปขั้นตอนที่เราทำ

1.  **ติดตั้ง:** ติดตั้ง `mdx-bundler` และ `gray-matter`.
2.  **Logic (lib/mdx.ts):** สร้าง Server Logic เพื่ออ่าน Slug, ดึง Content และ Bundling.
3.  **Dynamic Route (app/docs/[slug]/page.tsx):** ใช้ `generateStaticParams` และเรียก `bundleMDX` เพื่อสร้างหน้าแบบ Static/Dynamic.
4.  **Renderer (MdxRenderer.tsx):** สร้าง Client Component เพื่อเรนเดอร์โค้ด MDX ที่ถูก Bundle โดยใช้ `getMDXComponent`.
5.  **Configuration:** แก้ไข ESLint เพื่อให้รองรับ `catch (_)` ใน TypeScript.

> **คำแนะนำ:** Component ต่างๆ ที่ใช้ใน MDX (เช่น `<Alert>`) ควรอยู่ในรูปแบบของ Client Component หรือเป็น Server Component ที่ใช้ `use client` Wrapper เพื่อหลีกเลี่ยงข้อผิดพลาดเรื่อง Runtime Environment.

### การเรียกใช้ Component (Correct Syntax)

โปรดสังเกตว่าเราเรียกใช้ Component `Alert` ด้วย **JSX Tag** (`<Alert />`) ซึ่งเป็นวิธีที่ถูกต้องที่สุดในไฟล์ MDX ครับ.

## 🧪 ทดสอบ Code Block

นี่คือตัวอย่างโค้ด TypeScript ที่ใช้ใน Next.js:

```typescript
// Next.js Server Component Logic
import { getMdxContent } from '@/lib/mdx'; 

export async function getServerData(slug: string) {
    const data = await getMdxContent(slug);
    // ตรวจสอบการไฮไลต์ของ string และ keyword
    if (!data) {
        console.error("Content not found"); 
        return null;
    }
    return data.content; // return string
}
```